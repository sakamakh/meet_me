<nav class="navbar navbar-light navbar-expand-md fixed-top bg-white" style="box-shadow: 0px 0px 20px 0px #bfc0da;">
  <div class="container">
    <div class="col">
      <div class="d-flex justify-content-between align-items-center">
        <div>
          <%= link_to root_path, data: { turbolinks: false } do %>
            <%= image_tag 'logo.png', class: 'img-fluid', width: '150px' %>
          <% end %>
        </div>
        
        <div class="d-flex align-items-center">
          <% if user_signed_in? %>
            <%= link_to new_post_path, class: 'btn nav-btn text-white bg-main me-5 px-md-3 py-md-2', style: 'font-size: 14px;', data: { turbolinks: false } do %>
              <%= displayed_svg 'new_post.svg' %>
              新規投稿する
            <% end %>
            
            <%= image_tag current_user.avatar.url, class: 'rounded-circle flex-shrink-0 me-3 fit-cover', width: '50px', alt: 'icon6' %>
    
            <p class="pe-2 mt-3 text-purple d-none d-md-block" style="font-size: 14px;">アカウント名</p>
            <div class="dropdown" style="width: 10px;height: 8px;">
              <a class="dropdown-toggle text-decoration-none text-purple d-flex mt-1" aria-expanded="false" data-bs-toggle="dropdown" href="#" style="width: 10px;height: 8px;"></a>
              <div class="dropdown-menu dropdown-menu-end rounded" style="box-shadow: 0px 0px 10px #5B5F9C; padding: 4px 0px;">
                <%= link_to 'アカウント編集', edit_user_registration_path, class: 'dropdown-item text-main py-2 text-center', data: { turbolinks: false } %>
                <%= link_to 'ログアウト', destroy_user_session_path, class: 'dropdown-item text-main py-2 text-center', style: 'border-top: 1px solid #D5D5D5;', method: :delete %>
              </div>
            </div>
          <% else %>
            <%= link_to 'ログイン', new_user_session_path, class: 'btn nav-btn me-3', style: 'color: #259f94; text-decoration: underline;' ,data: { turbolinks: false } %>
            <%= link_to '新規会員登録', new_user_registration_path, class: 'btn nav-btn text-white btn-purple me-5 px-3 py-2', style: 'font-size: 14px;' 'background-color: #0097b2;', data: { turbolinks: false } %>
          <% end %>
        </div>
      </div>  
    </div>
  </div>
</nav>

